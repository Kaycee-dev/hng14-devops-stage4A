# Diagram Sources

Store editable diagram sources here.

Planned diagrams:

- manifest-to-generated-config dataflow
- `swiftdeploy init` rendering pipeline
- `deploy` sequence diagram
- `promote canary/stable` rolling restart sequence
- Nginx gateway and internal Docker network boundary
- chaos mode state machine
- security hardening model
- validation gate matrix

