# 2026-05-06 - Stage 4B Journal

## Goal

Extend the Stage 4A SwiftDeploy implementation with Stage 4B observability,
OPA policy enforcement, status, audit, proof, and blog updates.

## Operating Notes

- Stage 4A is the baseline; Stage 4B must extend it without erasing prior evidence.
- `task_details_4B.md` is internal prompt material and is ignored like the Stage 4A brief.
- Implementation proceeds through the new `SD4B-*` packet sequence.

## Teach-Back

OPA is the policy decision point: it answers structured questions using Rego.
The CLI is the policy enforcement point: it gathers facts, asks OPA, explains
the answer, and only then starts or promotes services. Metrics are cumulative
Prometheus samples, so the CLI derives rates and latency windows by comparing
snapshots rather than expecting the app to precompute every policy answer.

## SD4B-METRICS-001

- Added `/metrics` using Prometheus text format.
- Recorded request counters by method/path/status, duration histogram buckets,
  uptime, app mode, and chaos state.
- Kept `/metrics` reachable during chaos so policy checks can observe failure.
- Proof: `.venv-app\Scripts\python.exe scripts\smoke_app.py` passed with 0 failures.

## SD4B-POLICY-001

- Added `opa`, `policy`, and `observability` sections to the manifest.
- Added independent Rego domains: infrastructure and canary.
- Rendered the OPA service into Compose with a loopback host bind and read-only
  policy mount.
- Proof: Compose config validation passed and `python scripts/test_policies.py`
  passed with 0 failures.

## SD4B-CLI-001

- Replaced the large Bash body with a Bash entrypoint and Python helper package.
- Added OPA-gated `deploy` and `promote`, `status`, and `audit`.
- Fixed two implementation issues found during proof: nginx syntax validation
  needed a host entry for the Compose-only `app` DNS name, and OPA startup
  disconnects needed clean failure-mode handling.

## SD4B-PROOF-001

- Ran `C:\Program Files\Git\bin\bash.exe scripts/capture_evidence.sh`.
- Refreshed 11 Stage 4B proof files under `blog/assets/proof_outputs/`.

## SD4B-BLOG-001

- Updated README and `blog/devto/swiftdeploy-stage4b.md`.
- Added `stage4b_architecture.md` and `policy_gate_flow.md`.

## Grading-Prep Audit Remediation Session (2026-05-06 evening)

Ran a full grading-prep audit (`audit_review.md`) and worked through findings
F-001 through F-007 in priority order.

### F-001 (CRITICAL) — Committed all Stage 4B files (commit 0ee50e4)

All untracked and modified Stage 4B files (`swiftdeploy_lib/`, `policies/`,
proof outputs 02–11, article, diagrams, governance docs) were staged and
committed in one commit. The repo now contains the full Stage 4B implementation.

### F-006 (HIGH) — Fixed Python buffering in capture_evidence.sh

Added `export PYTHONUNBUFFERED=1` after `set -euo pipefail`. Re-ran the script.
`02_predeploy_policy_denial.txt` now shows the CLI banner before Docker events.

### F-004 (HIGH) — Added CPU policy denial proof (12_cpu_policy_denial.txt)

Added `set_cpu_threshold` helper (mirrors `set_disk_threshold`). Added CPU denial
capture block at end of script using threshold `-1.0` (Windows adaptation: since
`os.getloadavg()` is unavailable, `cpu_load` is always `0.0`; `-1.0 < 0.0`
triggers `0.0 > -1.0` → denial). Proof shows `cpu_load_too_high: cpu load 0 is
above allowed -1`.

### F-005 (HIGH) — Added nginx access log proof (13_nginx_access_log.txt)

Added capture block for `docker compose logs nginx --tail=30` placed between
`07_promote_stable.txt` and `08_opa_no_leakage.txt` (stack is running at that
point). Format verified: `$time_iso8601 | $status | ${request_time}s | ...`.

### F-007 (HIGH) — Expanded blog article

Rewrote `blog/devto/swiftdeploy-stage4b.md` from 62 lines to a full technical
deep-dive with: embedded Mermaid diagram, DESIGN section with manifest snippets
and template code, GUARDRAILS section with Rego and Python calls, CHAOS section
with verbatim terminal output, REPLICATION 10-step guide, LESSONS LEARNED with
4 lessons. `published: false` preserved.

### F-003 (CRITICAL) — Documented 30-second window trade-off

Implementation unchanged (regression risk). Documented in four places:
- README NOTE block under `promote` command
- Blog LESSONS LEARNED lesson (b)
- Interview defense bank Q72
- Decisions log D-025 (known limitation with trade-off and defense)

### New risks discovered

None. All findings are now fully documented or remediated. The only remaining
actions are operator-only: publish blog, push repo, submit form.
